import rules from 'ehealth/utils/rules';
import { module, test } from 'qunit';

module('Unit | Utility | rules');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rules();
  assert.ok(result);
});
