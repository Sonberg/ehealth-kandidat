
import { keyValueInArray } from 'ehealth/helpers/key-value-in-array';
import { module, test } from 'qunit';

module('Unit | Helper | key value in array');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = keyValueInArray([42]);
  assert.ok(result);
});

