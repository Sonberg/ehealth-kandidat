
import { isLastMessageByMe } from 'ehealth/helpers/is-last-message-by-me';
import { module, test } from 'qunit';

module('Unit | Helper | is last message by me');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isLastMessageByMe([42]);
  assert.ok(result);
});

